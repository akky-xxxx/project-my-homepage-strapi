{
  "name": "terraform",
  "main": "index.js",
  "scripts": {
    "check-all": "npm-run-all fix -p lint -s test",
    "dev-apply:service-strapi": "cd src/environments/dev && terraform apply",
    "dev-destroy:service-strapi": "cd src/environments/dev && terraform destroy",
    "dev-fix:service-strapi": "cd src/environments/dev && terraform fmt",
    "dev-lint:service-strapi": "cd src/environments/dev && terraform fmt -check",
    "dev-plan:service-strapi": "cd src/environments/dev && TF_VAR_run_sg_server=1 terraform plan",
    "dev-validate:service-strapi": "cd src/environments/dev && terraform validate",
    "fix": "run-p fix:*",
    "fix:pack": "fixpack",
    "fix:tf-dev": "run-p dev-fix:*",
    "lint": "run-p lint:*",
    "lint:pack": "fixpack --dryRun",
    "lint:tf:fmt-dev": "run-p dev-lint:*",
    "lint:tf:validate-dev": "run-p dev-validate",
    "test": "run-s test:*",
    "test:dev": "run-s dev-plan:*"
  },
  "devDependencies": {
    "fixpack": "^4.0.0",
    "npm-run-all": "^4.1.5"
  },
  "private": true,
  "version": "0.1.0",
  "license": "MIT"
}
